const Planet = ({ planet }) => {
  return (
    <div className="card">
      <h3>{planet.name}</h3>
      <p>Population: {planet.name}</p>
      <p>Terrain: {planet.name}</p>
    </div>
  );
};

export default Planet;
